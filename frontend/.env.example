# Copy to .env.local for local dev, .env.production for production build

# Leave empty in dev (Vite proxy handles /api → localhost:8000)
VITE_API_URL=

# Production example:
# VITE_API_URL=https://api.yourdomain.com
